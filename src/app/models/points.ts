import {Player} from './index';

export class Points {
    constructor(
        id: string,
        moves: number,
        time: number,
        player_id: string,
        player?: Player,
    ) {}
}
